env:
  node: true

extends: airbnb-base

overrides: []

parserOptions:
  ecmaVersion: latest
  sourceType: module
  
rules: {
  no-console: 0,
  import/extensions: 0,
  import/prefer-default-export: off | warn | error, 
}
